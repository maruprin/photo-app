import { createTheme } from "@mui/material";

export const paletaPagination = createTheme({
    palette: {
      primary: {
        // Purple and green play nicely together.
        main: '#ffffff',
      },
      secondary: {
        // This is green.A700 as hex.
        main: '#ffff99',
      },
    },
  });